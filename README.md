# conversor de moedas

Aplicação Python para conversão de valores entre moedas.

## Executar e validar

Use Python 3.12 ou compatível e execute o script principal. Antes de enviar uma mudança:

```bash
python -m compileall -q .
```

As cotações precisam informar fonte e data. Nunca inclua chaves de API no código.
